VITE_CLOUDINARY_CLOUD_NAME=dij1fbzfy
VITE_CLOUDINARY_UPLOAD_PRESET=posted_images
# Set to empty so baseURL becomes `/api` in the client. Avoid leading `/` which concatenates to `//api` and becomes a protocol-relative host.
VITE_API_URL=/
